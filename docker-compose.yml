services:
  app:
    build:
      context: .
    ports:
     - "8000:5000"
    volumes:
      -./app.py:app/app.py
      -./regression.joblib:app/regression.joblib
      -./requirements.txt:app/requirements.txt
